# Gold Tier MCP Server - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SMTP CONFIGURATION
# =============================================================================

# SMTP server hostname
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   Ethereal (testing): smtp.ethereal.email
SMTP_HOST=smtp.ethereal.email

# SMTP server port
# Common ports:
#   587 - TLS (recommended)
#   465 - SSL
#   25  - Unencrypted (not recommended)
SMTP_PORT=587

# Use secure connection (true for port 465, false for 587)
SMTP_SECURE=false

# SMTP authentication credentials
# For Gmail: Use an App Password (not your regular password)
# https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password

# Default "From" address for outgoing emails
SMTP_FROM=ai-employee@example.com

# =============================================================================
# LINKEDIN CONFIGURATION
# =============================================================================

# LinkedIn API credentials for posting
# Setup:
#   1. Go to https://developer.linkedin.com/
#   2. Create a new app
#   3. Add "Share on LinkedIn" product (under Products tab)
#   4. Generate an OAuth2 access token (under Auth tab)
#   5. Find your Person URN: use the LinkedIn API /v2/me endpoint
#
# Without these, post_linkedin runs in test/demo mode.
LINKEDIN_ACCESS_TOKEN=your-linkedin-access-token
LINKEDIN_PERSON_URN=urn:li:person:your-person-id

# =============================================================================
# ODOO ERP CONFIGURATION (Gold Tier)
# =============================================================================

# Odoo connection details for accounting integration
#
# Setup — Free Test Instance:
#   1. Go to https://www.odoo.com/trial
#   2. Create a free database (e.g., "mycompany")
#   3. Install the "Accounting" app from the Apps menu
#   4. Note your URL and database name
#
# Setup — API Key:
#   1. Log into your Odoo instance
#   2. Go to Settings > Users & Companies > Users
#   3. Click your user
#   4. Go to the "Account Security" tab (or "Preferences")
#   5. Under "API Keys", click "New API Key"
#   6. Give it a label (e.g., "AI Employee") and copy the key
#
# Without these, odoo_accounting runs in test/demo mode.

# Your Odoo instance URL (no trailing slash or /odoo or /web)
# Examples:
#   Odoo Online: https://mycompany.odoo.com
#   Self-hosted: https://erp.mycompany.com
ODOO_URL=https://your-instance.odoo.com

# Database name (usually the subdomain, e.g., "mycompany")
ODOO_DB=your-database-name

# Your Odoo login username (email)
ODOO_USERNAME=your-email@example.com

# API Key (NOT your login password — generate via Settings > API Keys)
ODOO_API_KEY=your-api-key-here

# =============================================================================
# X / TWITTER CONFIGURATION (Gold Tier — Social Watcher)
# =============================================================================

# X (Twitter) API bearer token for social_watcher.py
# Setup:
#   1. Go to https://developer.twitter.com/
#   2. Create a project and app
#   3. Generate a Bearer Token (under Keys and Tokens)
#
# Without this, social_watcher.py runs X in demo mode.
X_BEARER_TOKEN=your-x-bearer-token

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Enable test mode (logs emails instead of sending)
# Set to 'true' to test without actually sending emails
TEST_MODE=false
